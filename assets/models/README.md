This directory can optionally contain a higher-fidelity Orion model.

If present at `assets/models/orion.glb`, the viewer will load it and use it
instead of the procedural capsule mesh.

If the file is missing or fails to load, the app automatically falls back to
the procedural capsule without errors.
